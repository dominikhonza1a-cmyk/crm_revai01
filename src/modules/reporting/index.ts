/**
 * Modul REPORTING — dashboard = reporting. Read-only agregace pro widgety.
 * Soubory: widget-definition (katalog widgetů) · reporting.service (agregace) · reporting.router.
 * Přeskládatelné widgety + drill-down + per-user layout = UI fáze.
 */
export { reportingService } from "./reporting.service";
export { reportingRouter } from "./reporting.router";
export { WIDGET_CATALOG } from "./widget-definition";
