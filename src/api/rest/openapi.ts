/**
 * REST fasáda /api/v1/* pro n8n/Make/Zapier — generovaná ze STEJNÝCH zod schémat jako tRPC (jeden kontrakt).
 * OpenAPI spec je dostupná na /api/v1/openapi.json. Viz docs/integrations/rest-api.md.
 */
export function buildOpenApiSpec(): Record<string, unknown> {
  throw new Error("buildOpenApiSpec: generuj ze zod schémat modulů ve fázi 2–3 (trpc-openapi / zod-to-openapi).");
}
